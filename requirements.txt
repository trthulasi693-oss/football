# ====================================================================
# football 项目 - 云服务器部署依赖清单（完整版：crawler + Streamlit）
# ====================================================================
# 适用：云服务器同时跑爬虫和数据分析界面（app/）
# 用法：pip install -r requirements-full.txt
# ====================================================================

# ── crawler（爬虫）────────────────────────────────────────
requests>=2.31.0
SQLAlchemy>=2.0.0
APScheduler>=3.10.0

# ── app（Streamlit 后端）──────────────────────────────────
streamlit>=1.30.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# ── 缓存（Redis：跨进程共享分析结果）────────────────────
redis>=5.0.0

# ── 可选：PostgreSQL 驱动（换云数据库时启用）────────────
# psycopg2-binary>=2.9.0
